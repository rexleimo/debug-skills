interface:
  display_name: "Receiving Regression Review"
  short_description: "Close regression review findings"
  default_prompt: "Use $receiving-regression-review to consume a coverage-led regression-review report, build a disposition ledger for every finding, intentional change, and coverage gap, then verify, fix, challenge, or carry items forward with evidence."
