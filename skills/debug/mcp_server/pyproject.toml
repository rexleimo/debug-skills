[project]
name = "debug-mcp-server"
version = "0.1.0"
description = "MCP server for evidence-first runtime debugging"
requires-python = ">=3.10"
dependencies = ["mcp>=1.0.0"]

[project.scripts]
debug-mcp-server = "server:main"
