interface:
  display_name: "Runtime Debug"
  short_description: "Hypothesis-driven debugging with live logs and source jumps"
  default_prompt: "Use $debug to instrument this bug, capture runtime evidence, and prove the fix."
